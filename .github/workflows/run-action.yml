name: CI

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run the Action
        uses: fr-platform/github-action-get-repo-info@v0.0.2
        id: get-repo
